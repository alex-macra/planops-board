# Fictional demo repository

This directory is read-only example data for PlanOps Board. Copy it to a
temporary directory and initialize that copy as a Git repository before testing
any write, branch, or commit flow.

From the engine checkout, `npm run demo:init -- <destination>` creates that copy
with two fictional commits.

The names, repositories, tasks, dates, and plans are fictional.
